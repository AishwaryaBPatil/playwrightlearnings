let user = "abcd";
let password = "123@";
let isAccountLocked = true;

if (isAccountLocked) {

    if ((user === "abcd" && password === "123@") && isAccountLocked) {
        console.log("Login successful");
    }
    else {
        console.log("Login failed");
    }
}